# Exam Calendar Frontend

This is the React TypeScript frontend for Exam Calendar.

## Role
The frontend displays the user interface and communicates with the backend API.

## Local Development
The frontend runs separately from the backend during development.

## API Configuration
The frontend expects the backend API base URL to be configured through a Vite environment variable.